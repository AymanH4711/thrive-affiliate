cd D:\Ayman\Thrive\thrive-affiliate

# Create a clean, valid vercel.json
@"
{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
"@ | Out-File -FilePath vercel.json -Encoding UTF8 -NoNewline

# Commit and push
git add vercel.json
git commit -m "Fix: Remove invalid framework null value from vercel.json"
git push origin main